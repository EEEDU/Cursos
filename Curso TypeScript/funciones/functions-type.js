"use strict";
(() => {
    // const addNumbers = (a: number, b:number): number => {
    //     return a + b;
    // }
    const addNumbers = (a, b) => a + b;
    const saludar = (name) => "Hola ${name}";
    const saveTheWorld = () => "ok";
    let myFunction;
    myFunction = addNumbers;
    myFunction(1, 3);
})();
